# The script prints a list of items from a list (students) and adds a number before every item.

students = ["Hermione", "Harry", "Ron"]

for i in range(len(students)): # Len finds the length of the list = for i in range(3)
    print(i+1, students[i]) # [i] is the index of an item